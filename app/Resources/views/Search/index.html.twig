
    {{ form_start(form, {'action': path('product_search'), 'method': 'POST'}) }}
   
                        
                        <div class="table-responsive">
                            <table id="datatable-0" class="table">
                                <tbody>
                                    <tr>
                                        <td width="35%">{{ form_widget(form.search,{'attr': {'class': 'form-control search'}}) }}</td>
                                        <td style="color:red;text-align: left;">{{ form_errors(form.search) }}</td>
                                        <td>
                                            <button class="btn btn-primary" type="submit">{{ 'product.btn-search'|trans}}</button>
                                        </td>
                                    </tr>   
                                </tbody>
                            </table>
                        </div>                
                     
 {{ form_end(form) }}  
